# LIMITLESS LOYALTY — Bible Hub

This is a simple Next.js 15 app (single app/ folder) with local sample JSON data.

How to run locally:

1. npm install
2. npm run dev

Notes:
- No authentication, DBs, or external APIs — everything is local.
- Single app folder at /app
